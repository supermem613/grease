{
  "name": "grease",
  "version": "0.3.4",
  "description": "Copilot extension that captures friction and turns it into addressable work.",
  "type": "module",
  "private": true,
  "bin": {
    "grease": "./scripts/grease.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check.mjs && node scripts/sync-version.mjs --check",
    "test": "node --test",
    "setup": "node scripts/install-extension-shim.mjs",
    "sync-version": "node scripts/sync-version.mjs",
    "version": "node scripts/sync-version.mjs"
  },
  "keywords": [
    "copilot",
    "extension",
    "friction"
  ],
  "author": "Marcus Markiewicz",
  "license": "MIT"
}
